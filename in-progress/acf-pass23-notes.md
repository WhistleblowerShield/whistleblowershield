# Pass 23 Notes

- Double-check `label` attributes across all ACF fields for pluralization accuracy after key/name/meta refactors.
- Focus especially on taxonomy multi-select fields where labels may still read singular.
- Verify consistency between `label`, `field_type`, and naming (`*_types`, `*_classes`, `*_defenses`, etc.).
