[cite_start]The new prompt (**v20260414-0401**) represents a significant architectural leap in how it manages data uncertainty and researcher intent[cite: 736]. [cite_start]By moving from a "best-effort" model to a "high-confidence signaling" model, it creates a much more reliable bridge between the AI's research phase and your human review queue[cite: 741, 743].

Here are the key technical improvements in this version:

### 1. The Six-Tier Field Hierarchy
[cite_start]The introduction of the **Six Tiers of Requirements** (essential, expected, ternary, expected-if-found, conditional, optional) is the most critical update[cite: 759].
* [cite_start]**Essential Records**: By mandating that a record be omitted entirely if the name, URL, or general description is missing, you have successfully automated the "Gatekeeper" function, ensuring the database never contains "zombie" entries[cite: 759, 764].
* [cite_start]**Expected-if-Found**: Forcing the inclusion of a blank string `""` for fields like `nationwide_example` provides a clear signal that the researcher *checked* for evidence but found none, which is far more valuable for your review than a simple omission[cite: 772, 773].

### 2. Ternary Logic as a Review Trigger
[cite_start]The shift to **yes | no | unclear** for critical security and eligibility fields (like `has_secure_channel` and `income_eligibility_required`) solves the "binary hallucination" problem[cite: 769, 770].
* [cite_start]Using `unclear` as a deliberate signal rather than a failure allows the model to remain honest about the limitations of the source material while still delivering the rest of the verified record[cite: 770].
* [cite_start]This directly addresses your requirement for a "deeper human review" on complex cost structures[cite: 802].

### 3. Refined Cost Model Logic
[cite_start]The new instructions for `cost_models` now explicitly trigger the `unclear` sentinel when a model "does not fit the provided slugs cleanly"[cite: 802].
* In the most recent execution for **Whistleblowers of America**, this worked exactly as intended—the "Mixed" type was correctly abandoned in favor of the `unclear` slug because the peer-support landing page lacked explicit fee-for-service data.

### 4. "Secure Channel" Enforcement
[cite_start]The strict definition of a **Secure Channel** (ProtonMail, Signal, SecureDrop, etc.) specifically excludes standard HTTPS web forms[cite: 836, 837]. 
* [cite_start]This is a vital "James" safety feature[cite: 737].
* [cite_start]It prevents the directory from giving users a false sense of security when they are in an active retaliation state[cite: 740, 756].

### 5. The "Final Write Contract"
[cite_start]The addition of the **Final Write Contract** checklist acts as a last-second verification layer[cite: 923]. [cite_start]This reduces the likelihood of "memory drift" where the model might revert to older taxonomy terms (like the `retaliation-protection` slug we discussed) that are no longer valid[cite: 924, 925].

**Researcher Opinion**: This prompt is now "production-hardened." It moves away from the model trying to be "helpful" by guessing and instead forces it to be "accurate" by signaling. This structure effectively turns the AI into a forensic auditor of the organizations rather than just a data scraper.